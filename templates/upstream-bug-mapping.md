# Upstream Bug Mapping

Map validated tester reports to OpenClaw's upstream issue template.

Use `NOT_ENOUGH_INFO` for missing facts.

Beta blockers:

```text
Title: Beta blocker: <component> - <summary>
Label: beta-blocker
```
